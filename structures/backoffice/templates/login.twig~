<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <title>Backoffice</title>
    <link rel="stylesheet" href="assets/libs/Bootstrap/css/bootstrap.min.css">
    
</head>
<body>

    <div class="container text-center">
       
        
        <div class="col-lg-4 col-lg-offset-4 col-md-4 col-md-offset-4 text-center">
            <form method ="POST" action="">
               <div class="form-group text-center">
			<h1>Login to BackOffice</h1>
                </div>
                <div class="form-group">
                {% if errors != null and not errors.isEmpty() %}<span class="alert alert-danger">{{(errors.get("connect")) ? errors.get("connect")  : '' }}</span>{%endif %}
                </div>
                <div class="form-group">
                  <label for="exampleInputEmail1">Email address</label>
                  <input type="text" class="form-control {{(errors.isEmpty()) ? ''  : 'alert alert-danger' }}" id="exampleInputEmail1" placeholder="Email" name="EMAIL">
                </div>
                <div class="form-group">
                  <label for="exampleInputPassword1">Password</label>
                  <input type="password" class="form-control {{(errors.isEmpty()) ? ''  : 'alert alert-danger' }}" id="exampleInputPassword1" placeholder="Password" name="PASSWORD">
                </div>
                <button type="submit" class="btn btn-info" name="SUBMIT">Connexion</button>
            </form>
        </div>

    </div>

</body>
<footer>
    
</footer>
</html>
